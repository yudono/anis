import { http_get } from "http";
import { str_length } from "string";

println("Testing HTTP GET to google.com...");
const html = http_get("https://www.google.com");
if (str_length(html) > 0) {
    println("Success! Received " + str_length(html) + " bytes.");
} else {
    println("Failed to fetch.");
}
