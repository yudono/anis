// Web Server JSON Verification Script
import { Webserver } from "webserver";

const app = Webserver();

app.get("/json", (c) => {
    return c.json({
        message: "Hello World!",
        success: true,
        data: [1, 2, 3]
    });
});

println("Server running on http://localhost:3000");
println("Test command: curl http://localhost:3000/json");
app.listen({ port: 3000 });
